#include <stdio.h>

int main() {
    char str[1000];
    fgets(str, 1000, stdin);

    int count = 0;
    while (str[count] != '\0' && str[count] != '\n') {
        count++;
    }

    printf("%d", count);
    return 0;
}
