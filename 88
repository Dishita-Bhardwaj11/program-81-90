#include <stdio.h>

int main() {
    char str[1000];
    fgets(str, 1000, stdin);

    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] == ' ')
            printf("-");
        else if (str[i] != '\n')
            printf("%c", str[i]);
    }

    return 0;
}
